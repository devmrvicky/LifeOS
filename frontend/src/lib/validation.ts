// Re-exports the shared extraction schema/validator. See
// shared/schemas/extraction.ts for the actual implementation — kept in one
// place so frontend and server validation can never drift apart again.
export * from '@shared/schemas/extraction';
