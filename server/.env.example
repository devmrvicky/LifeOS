# Copy this file to server/.env and fill in a real key. Never commit .env.

# Required. Server-side only — this is never sent to or readable from the browser.
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional.
PORT=8787
NODE_ENV=production
